params {
    fasta = null // Path to your FASTA file (must be indexed)
    labels = null // Path to TSV file with coordinates (format: contig\tstart\tstop)
}

// Example usage:
// nextflow run main.nf --fasta genome.fasta --labels coordinates.tsv
